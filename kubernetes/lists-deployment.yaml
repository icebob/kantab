#########################################################
# lists service
#########################################################
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lists-deployment
  namespace: default
  labels:
    name: lists-deployment
    app: kantab
spec:
  selector:
    matchLabels:
      app: lists
  replicas: 2
  template:
    metadata:
      labels:
        app: lists
    spec:
      containers:
        - name: lists
          image: icebob/kantab:latest
          envFrom:
            - configMapRef:
                name: kantab-configmap
            - secretRef:
                name: kantab-secrets
          env:
            - name: SERVICES
              value: lists
          resources:
            limits:
              cpu: 200m
              memory: 500Mi
            requests:
              cpu: 50m
              memory: 40Mi
---
#########################################################
# Horizontal Pod AutoScaler for lists service (K8s >= v1.17)
#########################################################
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: hpa-lists
  namespace: default
  labels:
    name: hpa-lists
    app: kantab
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: lists-deployment
  minReplicas: 2
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80
---


